<div class="code-preview">
  <div class="code-header">
    <div class="code-dot"></div>
    <div class="code-dot"></div>
    <div class="code-dot"></div>
  </div>
  <div style="font-family: 'JetBrains Mono', monospace;" class="code-wrapper">
    <div style="color: var(--text-muted); margin-bottom: 1rem;">
      # English Track Benefits
    </div>
    <div style="color: var(--primary-light); margin-bottom: 0.5rem;">
      benefits = [
    </div>
    <div
      style="color: var(--accent); margin-left: 2rem; margin-bottom: 0.5rem;"
    >
      "Professional English",
    </div>
    <div
      style="color: var(--accent); margin-left: 2rem; margin-bottom: 0.5rem;"
    >
      "Global Career Opportunities",
    </div>
    <div
      style="color: var(--accent); margin-left: 2rem; margin-bottom: 0.5rem;"
    >
      "Native Tech Terminology",
    </div>
    <div style="color: var(--primary-light); margin-bottom: 1rem;">]</div>
    <div style="color: var(--secondary);">
      {`print(f"Level up: {len(benefits)} skills!")`}
    </div>
  </div>
</div>

<style>
  .code-preview {
    background: var(--code-bg);
    border-radius: 20px;
    padding: 2rem;
    border: 1px solid var(--border);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
    position: relative;
    overflow: hidden;
  }

  .code-header {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .code-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .code-dot:nth-child(1) {
    background: #ef4444;
  }
  .code-dot:nth-child(2) {
    background: #f59e0b;
  }
  .code-dot:nth-child(3) {
    background: #10b981;
  }

  .code-preview .code-wrapper {
    padding: 2rem 0;
  }

  .rotating-code {
    font-family: "JetBrains Mono", monospace;
    font-size: 1.3rem;
    color: var(--primary-light);
    font-weight: 600;
    min-height: 60px;
    display: flex;
    align-items: center;
    transition: opacity 0.3s ease;
  }

  @media (width < 768px) {
    .code-preview {
      padding: 1rem;
    }

    .code-preview .code-wrapper {
      padding: 0;
    }
  }
</style>
